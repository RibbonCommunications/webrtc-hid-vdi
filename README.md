# Ribbon's WebRTC HID Driver for VDI

As part of the [Distant VDI Toolkit](https://github.com/RibbonCommunications/webrtc-vdi-toolkit), the WebRTC HID Driver for VDI is an optional component that enables the use of HID devices in a Citrix VDI environment. The driver is used in conjunction with the [WebRTC HID SDK](https://github.com/RibbonCommunications/webrtc-hid-sdk) running on the VDI Virtual Desktop.

## Installation

This software is delivered in the form of eLux distribution packages. See documentation for your version of Citrix Workspace.

## Logging

Logs generated by this Driver are written to syslog and have a prefix of `VDIHID:`.

### RP6
Logs are written to /var/log/messages.

### RP7

By default syslog messages are managed by `journald` and can be seen using `journalctl`.

## Known Issues / Limitations

## Backwards Compatibility

When used in a Citrix VDI environment, the WebRTC HID SDK is backwards-compatible with the most recent and one (1) previous version of Driver (official releases only). This is intended to allow Driver upgrades on the Thin Client installed-base to lag behind application updates.

### Example:
#### Note these are fictional release values for purposes of illustration only; see the [SDK's compatibility matrix](https://github.com/RibbonCommunications/webrtc-hid-sdk/blob/master/docs/compatibility.md) for actual Driver and SDK version compatibility information.

| DLL Version | SDK Version |                  Explanation                      |
| :---------: | :---------: | :------------------------------------------------ |
| 1.0 | 1.0 | Initial release |
| -   | 1.1 | <ul><li>the HID SDK is updated</li><li>the HID Driver for VDI is not updated</li></ul> |
| 1.2 | 1.2 | <ul><li>the HID SDK is updated</li><li>the HID Driver for VDI is also updated</li><li>version 1.2 of the HID SDK is compatible with HID Driver for VDI versions 1.0 and 1.2</li></ul> |
| 1.3 | 1.3 | <ul><li>the HID SDK is updated</li><li>the HID Driver for VDI is also updated</li><li>version 1.3 of the HID SDK is compatible with HID Driver for VDI versions 1.2 and 1.3</li></ul> |


## CHANGELOG
See [CHANGELOG](./CHANGELOG.md).
